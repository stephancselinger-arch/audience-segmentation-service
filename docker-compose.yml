version: "3.9"

services:
  api:
    build: .
    ports:
      - "8001:8001"
    environment:
      - ENV=development
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
